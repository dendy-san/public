# Пошаговая инструкция: Деплой на VPS

## 📋 Шаг 1: Подготовка локального репозитория

### 1.1. Проверка текущего состояния

```bash
cd "D:\001\SEAREST\Генератор постов\public_gen\public"
git status
```

### 1.2. Добавление недостающих файлов (если нужно)

```bash
# Добавить .env.example как шаблон
git add .env.example
git commit -m "Добавлен шаблон .env.example"
```

---

## 📋 Шаг 2: Создание удаленного репозитория

### Вариант A: GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Название: `public-gen` (или любое другое)
4. Описание: "Генератор постов для сайтов"
5. Выберите **Private** (рекомендуется для проекта с API ключами)
6. **НЕ** добавляйте README, .gitignore, license
7. Нажмите "Create repository"

### Вариант B: GitLab

1. Перейдите на https://gitlab.com
2. Нажмите "New project"
3. Выберите "Create blank project"
4. Заполните название и описание
5. Выберите **Private**
6. Нажмите "Create project"

### Вариант C: Локальный сервер Git (без облака)

Если не хотите использовать облачные сервисы, можно:
- Использовать только локальный Git
- Копировать файлы на VPS через SCP/SFTP
- Использовать `rsync` для синхронизации

---

## 📋 Шаг 3: Подключение к удаленному репозиторию

### Если используете GitHub/GitLab:

```bash
# Замените <URL> на URL вашего репозитория
# Например: https://github.com/ваш_username/public-gen.git

git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
git branch -M main
git push -u origin main
```

**Пример:**
```bash
git remote add origin https://github.com/username/public-gen.git
git branch -M main
git push -u origin main
```

### Если НЕ используете удаленный репозиторий:

Пропустите этот шаг. В шаге 4 будет альтернативный способ переноса файлов.

---

## 📋 Шаг 4: Подготовка VPS

### 4.1. Подключение к VPS

```bash
# Замените user и vps_ip на ваши данные
ssh user@vps_ip
```

### 4.2. Установка необходимого ПО

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt install docker-compose -y

# Добавление пользователя в группу docker
sudo usermod -aG docker $USER
newgrp docker

# Установка Git (если используете удаленный репозиторий)
sudo apt install git -y
```

### 4.3. Создание директории для проекта

```bash
mkdir -p ~/projects
cd ~/projects
```

---

## 📋 Шаг 5: Копирование проекта на VPS

### Вариант A: Через Git (если настроен удаленный репозиторий)

```bash
cd ~/projects
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> public_gen
cd public_gen/public
```

### Вариант B: Через SCP (если НЕ используете Git)

**На локальной машине (Windows PowerShell):**

```powershell
# Перейти в директорию проекта
cd "D:\001\SEAREST\Генератор постов\public_gen\public"

# Копировать файлы на VPS (замените user и vps_ip)
scp -r * user@vps_ip:~/projects/public_gen/public/
```

**Или через WinSCP / FileZilla:**
- Подключитесь к VPS по SFTP
- Скопируйте все файлы из локальной директории в `~/projects/public_gen/public/` на VPS

---

## 📋 Шаг 6: Настройка .env на VPS

### 6.1. Создание .env файла

```bash
cd ~/projects/public_gen/public
nano .env
```

### 6.2. Заполнение переменных

Добавьте следующие переменные (замените на ваши реальные значения):

```env
# ========================================
# ГИБРИДНАЯ КОНФИГУРАЦИЯ LLM
# ========================================

# DeepSeek (основной - для глубокого анализа)
BASE_URL=https://api.deepseek.com/v1
API_KEY=sk-ваш_ключ_deepseek_здесь

# GPT-4o через ProxyAPI (альтернативный)
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
OPENAI_API_KEY=ваш_ключ_proxyapi_здесь

# ========================================
# ПАРАМЕТРЫ СИСТЕМЫ (из Redis, но можно задать по умолчанию)
# ========================================

# Продолжительность сеанса в минутах
W=60

# Цена за сеанс в рублях
Price=1000

# ID магазина YooKassa
ShopID=ваш_shop_id

# API ключ YooKassa
Ykassa_API=ваш_ykassa_api_key

# ========================================
# REDIS КОНФИГУРАЦИЯ
# ========================================

# Для Docker (не меняйте!)
REDIS_URL=redis://redis:6379

# ========================================
# БАЗА ДАННЫХ
# ========================================

# Путь к базе данных SQLite
DB_PATH=sessions.db
```

**Сохраните:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

## 📋 Шаг 7: Удаление старых контейнеров (если они есть)

### 7.1. Проверка существующих контейнеров

```bash
docker ps -a
```

### 7.2. Остановка и удаление старых контейнеров

```bash
# Если контейнеры запущены из другой директории
# Найдите их и остановите:

# Вариант 1: По имени контейнера
docker stop $(docker ps -aq --filter "name=public") 2>/dev/null || true
docker rm $(docker ps -aq --filter "name=public") 2>/dev/null || true

# Вариант 2: Все контейнеры (ОСТОРОЖНО!)
# docker stop $(docker ps -aq)
# docker rm $(docker ps -aq)
```

### 7.3. Удаление старых образов (опционально)

```bash
# Удалить неиспользуемые образы
docker image prune -a -f
```

---

## 📋 Шаг 8: Деплой новой версии

### 8.1. Переход в директорию проекта

```bash
cd ~/projects/public_gen/public
```

### 8.2. Запуск скрипта деплоя

```bash
# Сделать скрипт исполняемым
chmod +x deploy.sh

# Запустить деплой
./deploy.sh
```

**Скрипт автоматически:**
- ✅ Остановит и удалит старые контейнеры
- ✅ Удалит старые образы
- ✅ Обновит код из Git (если используется)
- ✅ Соберет новые образы
- ✅ Запустит новые контейнеры

### 8.3. Проверка работы

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f

# Проверка здоровья
curl http://localhost:8000/health
```

---

## 📋 Шаг 9: Настройка файрвола (если нужно)

```bash
# Открыть порты (если используете ufw)
sudo ufw allow 3000/tcp  # Frontend
sudo ufw allow 8000/tcp  # Backend
sudo ufw allow 8081/tcp  # Redis Commander (опционально)

# Проверить статус
sudo ufw status
```

---

## 📋 Шаг 10: Настройка домена (опционально)

Если у вас есть домен, настройте Nginx как reverse proxy:

```bash
# Установка Nginx
sudo apt install nginx -y

# Создание конфигурации
sudo nano /etc/nginx/sites-available/public_gen
```

Добавьте:

```nginx
server {
    listen 80;
    server_name ваш_домен.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

Активируйте:

```bash
sudo ln -s /etc/nginx/sites-available/public_gen /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

---

## 🔄 Обновление после изменений

### Локально:

```bash
# 1. Внести изменения
# 2. Закоммитить
git add .
git commit -m "Описание изменений"
git push origin main
```

### На VPS:

```bash
cd ~/projects/public_gen/public
git pull origin main
./deploy.sh
```

---

## ✅ Готово!

Ваше приложение должно быть доступно:
- Frontend: `http://ваш_vps_ip:3000`
- Backend: `http://ваш_vps_ip:8000`
- Redis Commander: `http://ваш_vps_ip:8081`

---

## 🆘 Решение проблем

### Проблема: "Permission denied" при запуске deploy.sh
**Решение:**
```bash
chmod +x deploy.sh
```

### Проблема: "Port already in use"
**Решение:**
```bash
# Найти процесс, использующий порт
sudo lsof -i :3000
sudo lsof -i :8000

# Остановить процесс или изменить порты в docker-compose.yml
```

### Проблема: Контейнеры не запускаются
**Решение:**
```bash
# Проверить логи
docker-compose logs

# Проверить конфигурацию
docker-compose config
```

---

## 📞 Следующие шаги

После успешного деплоя:
1. ✅ Проверьте работу всех сервисов
2. ✅ Настройте мониторинг (опционально)
3. ✅ Настройте автоматические бэкапы БД
4. ✅ Настройте SSL сертификат (Let's Encrypt)

