# 🌐 ИНСТРУКЦИЯ ПО ДОСТУПУ К ПРИЛОЖЕНИЮ В ИНТЕРНЕТЕ

После успешного деплоя ваше приложение доступно в интернете через VPS.

## 🔗 Доступ к сервисам

### Основные сервисы:

1. **Frontend (Основное приложение)**
   ```
   http://ВАШ_IP_АДРЕС:3000
   ```
   Пример: `http://123.45.67.89:3000`

2. **Backend API**
   ```
   http://ВАШ_IP_АДРЕС:8000
   ```
   Пример: `http://123.45.67.89:8000`
   - API документация: `http://ВАШ_IP_АДРЕС:8000/docs`
   - Health check: `http://ВАШ_IP_АДРЕС:8000/health`

3. **Admin Frontend (Админ-панель)**
   ```
   http://ВАШ_IP_АДРЕС:3001
   ```
   Пример: `http://123.45.67.89:3001`

4. **Redis Commander (Управление Redis)**
   ```
   http://ВАШ_IP_АДРЕС:8081
   ```
   Пример: `http://123.45.67.89:8081`

## 📋 Как узнать IP адрес вашего VPS

### Вариант 1: Из GitHub Secrets
IP адрес хранится в секрете `VPS_HOST` в настройках репозитория GitHub.

### Вариант 2: Через SSH
Подключитесь к VPS и выполните:
```bash
curl ifconfig.me
# или
hostname -I
```

## 🔐 Проверка работы приложения

### 1. Проверка Backend
```bash
curl http://ВАШ_IP_АДРЕС:8000/health
```
Должен вернуть: `{"status":"healthy"}`

### 2. Проверка Frontend
Откройте в браузере: `http://ВАШ_IP_АДРЕС:3000`

### 3. Проверка Admin панели
Откройте в браузере: `http://ВАШ_IP_АДРЕС:3001`

## 🌍 Настройка доменного имени (опционально)

Если у вас есть домен, вы можете настроить его для доступа к приложению.

### Вариант 1: Прямое подключение домена

1. Настройте A-запись у вашего регистратора домена:
   ```
   A запись: @ → IP_АДРЕС_VPS
   ```

2. Используйте домен для доступа:
   ```
   http://ваш-домен.com:3000  (Frontend)
   http://ваш-домен.com:8000  (Backend)
   ```

### Вариант 2: Настройка Nginx Reverse Proxy (рекомендуется)

Это позволит убрать номера портов из URL и использовать стандартные порты 80/443.

**На VPS установите Nginx:**
```bash
sudo apt update
sudo apt install nginx -y
```

**Создайте конфигурацию `/etc/nginx/sites-available/public`:**
```nginx
# Frontend
server {
    listen 80;
    server_name ваш-домен.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Backend API
server {
    listen 80;
    server_name api.ваш-домен.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Admin панель
server {
    listen 80;
    server_name admin.ваш-домен.com;

    location / {
        proxy_pass http://localhost:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Активируйте конфигурацию:**
```bash
sudo ln -s /etc/nginx/sites-available/public /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

**Настройте DNS записи:**
```
A запись: @ → IP_АДРЕС_VPS
A запись: api → IP_АДРЕС_VPS
A запись: admin → IP_АДРЕС_VPS
```

## 🔒 Настройка HTTPS (SSL сертификат)

Для безопасного доступа рекомендуется настроить SSL сертификат через Let's Encrypt.

**Установите Certbot:**
```bash
sudo apt install certbot python3-certbot-nginx -y
```

**Получите сертификат:**
```bash
sudo certbot --nginx -d ваш-домен.com -d api.ваш-домен.com -d admin.ваш-домен.com
```

После этого доступ будет по HTTPS:
- `https://ваш-домен.com`
- `https://api.ваш-домен.com`
- `https://admin.ваш-домен.com`

## 🔥 Настройка Firewall

Убедитесь, что нужные порты открыты в firewall:

```bash
# Ubuntu/Debian (ufw)
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 80/tcp    # HTTP (если используете Nginx)
sudo ufw allow 443/tcp   # HTTPS (если используете SSL)
sudo ufw allow 3000/tcp  # Frontend
sudo ufw allow 8000/tcp  # Backend
sudo ufw allow 3001/tcp  # Admin
sudo ufw enable

# Или для Cloudflare/proxy - не нужно открывать порты,
# доступ будет через Cloudflare
```

## ✅ Проверка статуса контейнеров

Подключитесь к VPS и выполните:
```bash
ssh -p ПОРТ_SSH ПОЛЬЗОВАТЕЛЬ@IP_АДРЕС
cd /root/public
docker compose ps
```

Все контейнеры должны быть в статусе `Up` и `healthy`.

## 📊 Мониторинг

### Просмотр логов:
```bash
# Логи всех сервисов
docker compose logs -f

# Логи конкретного сервиса
docker compose logs -f backend
docker compose logs -f frontend
```

### Проверка использования ресурсов:
```bash
docker stats
```

## 🐛 Устранение проблем

### Приложение недоступно

1. **Проверьте, что контейнеры запущены:**
   ```bash
   docker compose ps
   ```

2. **Проверьте логи:**
   ```bash
   docker compose logs backend
   docker compose logs frontend
   ```

3. **Проверьте firewall:**
   ```bash
   sudo ufw status
   ```

4. **Проверьте, что порты прослушиваются:**
   ```bash
   sudo netstat -tlnp | grep -E '3000|8000|3001'
   ```

### 502 Bad Gateway (при использовании Nginx)

- Проверьте, что контейнеры запущены
- Проверьте конфигурацию Nginx
- Проверьте логи Nginx: `sudo tail -f /var/log/nginx/error.log`

## 📝 Быстрые команды

```bash
# Перезапуск всех сервисов
cd /root/public
docker compose restart

# Перезапуск конкретного сервиса
docker compose restart backend

# Обновление кода (деплой через Git)
cd /root/public
git pull origin main
docker compose up -d --build

# Просмотр статуса
docker compose ps

# Просмотр логов в реальном времени
docker compose logs -f
```

## 🎉 Готово!

Ваше приложение теперь доступно в интернете! 

**Основные URL:**
- Frontend: `http://ВАШ_IP:3000`
- Backend API: `http://ВАШ_IP:8000`
- Admin: `http://ВАШ_IP:3001`

Делитесь ссылками с пользователями! 🚀
