name: Deploy to VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.VPS_SSH_PORT || 22 }} ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to VPS
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          VPS_SSH_PORT: ${{ secrets.VPS_SSH_PORT || 22 }}
          VPS_PROJECT_PATH: ${{ secrets.VPS_PROJECT_PATH || '~/public' }}
          BACKEND_PORT: ${{ secrets.BACKEND_PORT || 8000 }}
          FRONTEND_PORT: ${{ secrets.FRONTEND_PORT || 3000 }}
          ADMIN_FRONTEND_PORT: ${{ secrets.ADMIN_FRONTEND_PORT || 3001 }}
        run: |
          ssh -i ~/.ssh/deploy_key -p $VPS_SSH_PORT $VPS_USER@$VPS_HOST << 'ENDSSH'
            set -e
            set +H  # Отключаем history expansion для избежания проблем с !
            
            PROJECT_PATH="${{ secrets.VPS_PROJECT_PATH || '~/public' }}"
            PROJECT_PATH=$(eval echo $PROJECT_PATH)
            
            echo "🚀 Начало деплоя на VPS..."
            echo "📁 Путь к проекту: $PROJECT_PATH"
            
            # Переход в директорию проекта
            cd "$PROJECT_PATH" || { echo "❌ Директория не найдена: $PROJECT_PATH"; exit 1; }
            
            # Сохранение текущей версии для rollback
            CURRENT_COMMIT=$(git rev-parse HEAD 2>/dev/null || echo "unknown")
            if [ "$CURRENT_COMMIT" != "unknown" ]; then
              echo "$CURRENT_COMMIT" > .previous_version
              echo "💾 Текущая версия сохранена: $CURRENT_COMMIT"
            else
              echo "⚠️ Не удалось получить текущий коммит"
            fi
            
            # Получение последних изменений
            echo "📥 Получение изменений из Git..."
            git fetch origin
            git pull origin main || { echo "❌ Ошибка при git pull"; exit 1; }
            
            # Агрессивная очистка Docker перед деплоем
            echo "🧹 Очистка Docker перед деплоем..."
            docker compose down --remove-orphans 2>/dev/null || true
            sleep 2
            CONTAINERS=$(docker ps -aq --filter "name=public-" 2>/dev/null || echo "")
            if [ -n "$CONTAINERS" ] && [ "$CONTAINERS" != "" ]; then
              echo "$CONTAINERS" | xargs -r docker stop 2>/dev/null || true
              sleep 1
              echo "$CONTAINERS" | xargs -r docker rm -f 2>/dev/null || true
            fi
            # Удаление всех образов с тегами public-
            PUBLIC_IMAGES=$(docker images --format "{{.Repository}}:{{.Tag}}" 2>/dev/null | grep "^public-" 2>/dev/null || true)
            if [ -n "$PUBLIC_IMAGES" ]; then
              echo "$PUBLIC_IMAGES" | while IFS= read -r img || [ -n "$img" ]; do
                if [ -n "$img" ] && [ "$img" != "" ]; then
                  docker rmi -f "$img" 2>/dev/null || true
                fi
              done
            fi
            docker network rm public_public-network 2>/dev/null || true
            docker network prune -f 2>/dev/null || true
            # Очистка build cache
            docker builder prune -af 2>/dev/null || true
            sleep 2
            
            # Пересборка образов
            echo "🔨 Пересборка Docker образов..."
            DEPLOY_ID=$(date +%s)
            export DEPLOY_ID
            UNIQUE_TAG="deploy-${DEPLOY_ID}"
            export UNIQUE_TAG
            # Пересборка с повторными попытками
            BUILD_RETRIES=3
            BUILD_SUCCESS=false
            for i in $(seq 1 $BUILD_RETRIES); do
              echo "🔨 Попытка сборки $i из $BUILD_RETRIES..."
              if docker compose build --no-cache; then
                BUILD_SUCCESS=true
                break
              else
                echo "⚠️ Попытка $i не удалась, повтор через 5 секунд..."
                sleep 5
                # Дополнительная очистка перед повторной попыткой
                docker builder prune -af 2>/dev/null || true
              fi
            done
            if [ "$BUILD_SUCCESS" = "false" ]; then
              echo "❌ Ошибка при сборке образов после $BUILD_RETRIES попыток"
              exit 1
            fi
            
            # Запуск контейнеров
            echo "🚀 Запуск контейнеров..."
            docker compose up -d || { echo "❌ Ошибка при запуске контейнеров"; exit 1; }
            
            # Ожидание запуска
            echo "⏳ Ожидание запуска контейнеров (30 секунд для инициализации)..."
            sleep 30
            
            # Проверка здоровья Redis
            echo "🔍 Проверка здоровья Redis..."
            if docker compose ps redis 2>/dev/null | grep -q "healthy"; then
              echo "✅ Redis здоров"
            else
              echo "⚠️ Redis не здоров, но продолжаем..."
            fi
            
            # Проверка здоровья Backend
            echo "🔍 Проверка здоровья Backend..."
            MAX_RETRIES=10
            RETRY_COUNT=0
            BACKEND_HEALTHY=false
            
            while [ $RETRY_COUNT -lt $MAX_RETRIES ]; do
              # Используем имя сервиса из docker-compose, а не имя контейнера
              if docker compose exec -T backend curl -f http://localhost:8000/health > /dev/null 2>&1; then
                echo "✅ Backend здоров"
                BACKEND_HEALTHY=true
                break
              fi
              RETRY_COUNT=$((RETRY_COUNT + 1))
              echo "⏳ Ожидание Backend... ($RETRY_COUNT/$MAX_RETRIES)"
              sleep 5
            done
            
            if [ "$BACKEND_HEALTHY" = "false" ]; then
              echo "❌ Backend не прошел health check"
              echo "📋 Логи Backend:"
              docker compose logs --tail=50 backend
              exit 1
            fi
            
            # Проверка статуса контейнеров
            echo "📊 Статус контейнеров:"
            docker compose ps
            
            # Очистка Docker
            echo "🧹 Очистка Docker..."
            docker system prune -af --volumes || echo "⚠️ Ошибка при очистке Docker"
            
            echo "✅ Деплой завершен успешно!"
          ENDSSH

      - name: Send Telegram notification (success)
        if: success()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_CHAT_ID" ] && [ "$TELEGRAM_BOT_TOKEN" != "none" ] && [ "$TELEGRAM_CHAT_ID" != "none" ] && [ "$TELEGRAM_BOT_TOKEN" != "" ] && [ "$TELEGRAM_CHAT_ID" != "" ]; then
            MESSAGE="✅ Деплой успешно завершен!%0A%0AРепозиторий: ${{ github.repository }}%0AВетка: ${{ github.ref_name }}%0AКоммит: ${{ github.sha }}%0AАвтор: ${{ github.actor }}"
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d "chat_id=${TELEGRAM_CHAT_ID}" \
              -d "text=${MESSAGE}" \
              -d "parse_mode=HTML" > /dev/null || echo "⚠️ Не удалось отправить уведомление в Telegram"
          else
            echo "ℹ️ Telegram уведомления пропущены (не настроены)"
          fi

      - name: Send Telegram notification (failure)
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_CHAT_ID" ] && [ "$TELEGRAM_BOT_TOKEN" != "none" ] && [ "$TELEGRAM_CHAT_ID" != "none" ] && [ "$TELEGRAM_BOT_TOKEN" != "" ] && [ "$TELEGRAM_CHAT_ID" != "" ]; then
            MESSAGE="❌ Деплой завершился с ошибкой!%0A%0AРепозиторий: ${{ github.repository }}%0AВетка: ${{ github.ref_name }}%0AКоммит: ${{ github.sha }}%0AАвтор: ${{ github.actor }}%0A%0AПроверьте логи в GitHub Actions."
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d "chat_id=${TELEGRAM_CHAT_ID}" \
              -d "text=${MESSAGE}" \
              -d "parse_mode=HTML" > /dev/null || echo "⚠️ Не удалось отправить уведомление в Telegram"
          else
            echo "ℹ️ Telegram уведомления пропущены (не настроены)"
          fi
