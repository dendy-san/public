# 🚀 GitHub Actions - Настройка автоматического деплоя на VPS

Этот документ описывает настройку автоматического деплоя на VPS при каждом push в ветку `main`.

## 📋 Предварительные требования

1. **VPS сервер** с установленными:
   - Git
   - Docker
   - Docker Compose
   - SSH доступ

2. **GitHub репозиторий** с включенными GitHub Actions

3. **SSH ключ** для доступа к VPS

## 🔑 Настройка секретов в GitHub

Необходимо добавить следующие секреты в настройках репозитория GitHub:

**Путь:** `Settings` → `Secrets and variables` → `Actions` → `New repository secret`

### Обязательные секреты:

1. **`VPS_HOST`** - IP адрес или домен вашего VPS
   - Пример: `192.168.1.100` или `example.com`

2. **`VPS_USER`** - Имя пользователя для SSH подключения
   - Пример: `root` или `deploy`

3. **`VPS_SSH_KEY`** - Приватный SSH ключ для подключения к VPS
   - Полный содержимое приватного ключа (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)
   - Не используйте ключ с passphrase

### Опциональные секреты (со значениями по умолчанию):

4. **`VPS_SSH_PORT`** - SSH порт (по умолчанию: `22`)
   - Пример: `2222` (если используется нестандартный порт)

5. **`VPS_PROJECT_PATH`** - Путь к проекту на VPS (по умолчанию: `~/public`)
   - Пример: `/home/user/public` или `/var/www/public`

6. **`BACKEND_PORT`** - Порт Backend API (по умолчанию: `8000`)
   - Пример: `8000`

7. **`FRONTEND_PORT`** - Порт Frontend (по умолчанию: `3000`)
   - Пример: `3000`

8. **`ADMIN_FRONTEND_PORT`** - Порт Admin Frontend (по умолчанию: `3001`)
   - Пример: `3001`

## 🔐 Генерация SSH ключа

Если у вас еще нет SSH ключа:

```bash
# Генерация нового SSH ключа
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/github_actions_deploy

# Вывод публичного ключа (нужно добавить на VPS)
cat ~/.ssh/github_actions_deploy.pub

# Вывод приватного ключа (нужно добавить как секрет VPS_SSH_KEY в GitHub)
cat ~/.ssh/github_actions_deploy
```

## 📤 Добавление публичного ключа на VPS

Добавьте публичный ключ на VPS для разрешения подключения:

```bash
# На VPS выполните:
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ в authorized_keys
echo "ваш_публичный_ключ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

Или используйте команду:

```bash
# На локальной машине (если ключ уже сгенерирован)
ssh-copy-id -i ~/.ssh/github_actions_deploy.pub user@your-vps-host
```

## 🧪 Тестирование SSH подключения

Проверьте, что подключение работает:

```bash
# Подключение с использованием приватного ключа
ssh -i ~/.ssh/github_actions_deploy -p 22 user@your-vps-host

# Если подключение успешно, вы увидите приглашение shell на VPS
```

## 🚀 Первая настройка проекта на VPS

Убедитесь, что проект склонирован на VPS:

```bash
# На VPS выполните:
cd ~/  # или другой путь, указанный в VPS_PROJECT_PATH
git clone https://github.com/your-username/your-repo.git public
cd public

# Создайте .env файл (если еще не создан)
cp .env.example .env
# Отредактируйте .env и заполните все необходимые переменные

# Убедитесь, что ENVIRONMENT=prod в .env
echo "ENVIRONMENT=prod" >> .env
```

## 🔄 Как работает автоматический деплой

При каждом push в ветку `main` GitHub Actions автоматически:

1. ✅ Подключается к VPS через SSH
2. ✅ Переходит в директорию проекта
3. ✅ Выполняет `git pull origin main`
4. ✅ Останавливает текущие контейнеры (`docker compose down`)
5. ✅ Пересобирает Docker образы (`docker compose build --no-cache`)
6. ✅ Запускает контейнеры (`docker compose up -d`)
7. ✅ Ждет 60 секунд для запуска сервисов
8. ✅ Проверяет здоровье всех сервисов:
   - Backend: `http://VPS_HOST:8000/health`
   - Frontend: `http://VPS_HOST:3000`
   - Admin Frontend: `http://VPS_HOST:3001`

## 📊 Мониторинг деплоев

Проверить статус деплоев можно в GitHub:

**Путь:** `Actions` → выберите workflow `Deploy to VPS` → выберите нужный run

Вы увидите:
- ✅ Успешный деплой (зеленая галочка)
- ❌ Неудачный деплой (красный крестик) с деталями ошибки

## 🐛 Устранение проблем

### Ошибка: "Permission denied (publickey)"

**Причина:** SSH ключ не добавлен на VPS или неправильный.

**Решение:**
1. Проверьте, что публичный ключ добавлен в `~/.ssh/authorized_keys` на VPS
2. Убедитесь, что правильные права на файлы: `chmod 700 ~/.ssh` и `chmod 600 ~/.ssh/authorized_keys`
3. Проверьте, что приватный ключ правильно скопирован в секрет `VPS_SSH_KEY` в GitHub

### Ошибка: "docker-compose.yml не найден"

**Причина:** Неправильный путь к проекту на VPS.

**Решение:**
1. Проверьте секрет `VPS_PROJECT_PATH` в GitHub
2. Убедитесь, что путь существует на VPS: `ssh user@vps "ls -la /path/to/project"`

### Ошибка: "Backend не отвечает"

**Причина:** Backend не запустился или порт недоступен.

**Решение:**
1. Проверьте логи на VPS: `docker compose logs backend`
2. Убедитесь, что порт `BACKEND_PORT` открыт в firewall
3. Проверьте, что в `.env` установлено `ENVIRONMENT=prod`

### Ошибка: "Ошибка при пересборке образов"

**Причина:** Проблемы с Docker или нехватка ресурсов.

**Решение:**
1. Проверьте логи на VPS: `docker compose build --no-cache`
2. Убедитесь, что на VPS достаточно места: `df -h`
3. Проверьте статус Docker: `docker info`

## 🔒 Безопасность

- ✅ **Никогда не коммитьте** приватные SSH ключи в репозиторий
- ✅ Используйте **отдельный SSH ключ** только для GitHub Actions (не ваш основной ключ)
- ✅ Регулярно **ротируйте SSH ключи** (меняйте их периодически)
- ✅ Используйте **непривилегированного пользователя** для деплоя (не root, если возможно)
- ✅ Ограничьте права SSH ключа только необходимыми командами (если возможно)

## 📝 Пример полной настройки

```bash
# 1. На локальной машине: генерация ключа
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions
cat ~/.ssh/github_actions.pub

# 2. На VPS: добавление ключа
ssh user@vps
mkdir -p ~/.ssh && chmod 700 ~/.ssh
echo "ваш_публичный_ключ" >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# 3. В GitHub: добавление секретов
# Settings → Secrets → New repository secret
# VPS_HOST = your-vps-ip
# VPS_USER = your-username
# VPS_SSH_KEY = (содержимое ~/.ssh/github_actions)
# VPS_PROJECT_PATH = /home/user/public

# 4. На VPS: клонирование проекта
git clone https://github.com/your-username/your-repo.git ~/public
cd ~/public
cp .env.example .env
# Отредактируйте .env

# 5. Тест: push в main ветку
git push origin main
# Проверьте в GitHub Actions, что деплой прошел успешно
```

## ✅ Проверка после настройки

После настройки выполните тестовый push в `main` ветку:

```bash
# Сделайте любое небольшое изменение
echo "# Test" >> README.md
git add README.md
git commit -m "Test GitHub Actions deploy"
git push origin main
```

Затем проверьте в GitHub:
- `Actions` → должен появиться workflow `Deploy to VPS`
- Дождитесь завершения (зеленая галочка)
- Проверьте, что сервисы доступны на VPS

---

**Готово!** Теперь при каждом push в `main` ветку автоматически выполняется деплой на VPS. 🎉

