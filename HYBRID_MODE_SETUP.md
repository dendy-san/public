# 🔀 ГИБРИДНЫЙ РЕЖИМ LLM - НАСТРОЙКА

## 📋 Что это?

Гибридный режим использует **два LLM провайдера одновременно**:
- **DeepSeek-V3** - для анализа больших текстов (экономия ~94% токенов)
- **GPT-4o** - для быстрых chat/JSON запросов (скорость <2 сек)

## 💰 Экономия

| Метрика | Только GPT-4o | Гибрид | Экономия |
|---------|---------------|--------|----------|
| Стоимость/месяц | $350-525 | $21-35 | **94%** |
| Скорость chat/JSON | <2 сек | <2 сек | ✅ |
| Скорость анализа | 10-16 сек | 27 сек | -11 сек |

**Вывод:** Экономия $300-490/месяц за счёт замедления анализа на ~10-17 секунд.

## 🔧 Настройка .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# ГИБРИДНАЯ КОНФИГУРАЦИЯ LLM

# Основной провайдер - DeepSeek (для анализа больших текстов)
BASE_URL=https://api.deepseek.com/v1
API_KEY=ваш_ключ_deepseek_v3

# Альтернативный провайдер - GPT-4o (для быстрых chat/JSON)
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
OPENAI_API_KEY=ваш_ключ_proxyapi

# Redis (для кэширования и фоновых задач)
REDIS_URL=redis://localhost:6379
```

## 🎯 Как работает распределение?

### GPT-4o используется для:
1. **`get_steps()`** - быстрое получение плана анализа (JSON)
2. **`finalize()`** - быстрая генерация постов (JSON)
3. **`/chat`** и **`/chat-json`** эндпоинты

### DeepSeek используется для:
1. **`run_step()`** - глубокий анализ больших текстов сайта
2. Все запросы с большим контекстом (экономия токенов)

## 🚀 Запуск

### Вариант 1: Docker (рекомендуется)
```bash
# 1. Убедитесь, что .env настроен
# 2. Запустите контейнеры
docker-compose up --build -d

# 3. Проверьте логи
docker-compose logs -f backend

# 4. Откройте приложение
# Frontend: http://localhost:3000
# Backend: http://localhost:8000
```

### Вариант 2: Локально
```bash
# 1. Запустите Redis
docker run -d -p 6379:6379 redis:7-alpine

# 2. Запустите backend
cd backend
source venv/bin/activate  # или venv\Scripts\activate на Windows
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000

# 3. Запустите frontend
cd frontend
npm run dev
```

## ✅ Проверка работы

При запуске backend вы должны увидеть в логах:
```
[LLMClient] Hybrid mode: Primary=deepseek-chat, Alternative=gpt-4o
```

## 📊 Мониторинг использования

- **DeepSeek Dashboard:** https://platform.deepseek.com/usage
- **ProxyAPI Dashboard:** https://proxyapi.ru/dashboard

## 🔄 Переключение режимов

### Вернуться к только GPT-4o:
В `.env` закомментируйте DeepSeek:
```env
# BASE_URL=https://api.deepseek.com/v1
# API_KEY=ваш_ключ_deepseek_v3

OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
OPENAI_API_KEY=ваш_ключ_proxyapi
```

### Использовать только DeepSeek:
В `.env` закомментируйте GPT-4o:
```env
BASE_URL=https://api.deepseek.com/v1
API_KEY=ваш_ключ_deepseek_v3

# OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
# OPENAI_API_KEY=ваш_ключ_proxyapi
```

## 🐛 Устранение проблем

### Backend не запускается
```bash
# Проверьте наличие __init__.py файлов
ls app/__init__.py
ls app/routers/__init__.py
ls app/services/__init__.py

# Если отсутствуют - создайте:
touch app/__init__.py app/routers/__init__.py app/services/__init__.py
```

### Redis недоступен
```bash
# Проверьте, запущен ли Redis
docker ps | grep redis

# Или локально
redis-cli ping
```

### Ошибка "Insufficient Balance"
Пополните баланс на:
- **DeepSeek:** https://platform.deepseek.com/top_up
- **ProxyAPI:** https://proxyapi.ru/balance

## 📝 Код изменений

Гибридный режим реализован через параметр `use_alt`:
- `use_alt=True` → использует GPT-4o (альтернативный клиент)
- `use_alt=False` → использует DeepSeek (основной клиент)

Пример:
```python
# Быстрый JSON с GPT-4o
result = await llm.chat_json(..., use_alt=True)

# Анализ больших текстов с DeepSeek (экономия)
result = await llm.chat_json(..., use_alt=False)
```





