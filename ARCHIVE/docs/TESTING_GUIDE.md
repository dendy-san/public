# 🧪 РУКОВОДСТВО ПО ТЕСТИРОВАНИЮ

## 📋 Содержание

1. [Подготовка к тестированию](#подготовка-к-тестированию)
2. [Быстрый старт](#быстрый-старт)
3. [Автоматические тесты](#автоматические-тесты)
4. [Ручное тестирование](#ручное-тестирование)
5. [Проверка функциональности](#проверка-функциональности)
6. [Устранение проблем](#устранение-проблем)

---

## 🔧 Подготовка к тестированию

### 1. Проверьте наличие .env файла

Убедитесь, что файл `.env` существует в корне проекта и содержит:

```env
# DeepSeek (основной)
BASE_URL=https://api.deepseek.com/v1
API_KEY=ваш_ключ_deepseek

# GPT-4o через ProxyAPI (альтернативный)
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1
OPENAI_API_KEY=ваш_ключ_proxyapi

# Redis
REDIS_URL=redis://redis:6379
```

### 2. Установите зависимости для тестирования

```bash
# Backend зависимости (если тестируете локально)
pip install -r requirements.txt

# Для тестового скрипта
pip install requests
```

---

## 🚀 Быстрый старт

### Вариант 1: Docker (рекомендуется)

```bash
# 1. Запустите все сервисы
docker-compose up --build -d

# 2. Проверьте статус контейнеров
docker-compose ps

# 3. Проверьте логи
docker-compose logs -f backend

# 4. Откройте в браузере
# Frontend: http://localhost:3000
# Backend API: http://localhost:8000/docs
```

### Вариант 2: Локальный запуск

```bash
# Терминал 1: Redis
docker run -d -p 6379:6379 redis:7-alpine

# Терминал 2: Backend
cd /path/to/project
source venv/bin/activate  # или venv\Scripts\activate на Windows
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# Терминал 3: Frontend
cd frontend
npm install
npm run dev
```

---

## 🤖 Автоматические тесты

### Запуск тестового скрипта

```bash
# Убедитесь, что backend запущен
python test_api.py
```

### Что проверяет скрипт:

1. ✅ **Health Check** - доступность сервера
2. ✅ **Root Endpoint** - базовый endpoint
3. ✅ **Session Check** - проверка сеансов
4. ✅ **Chat Endpoint** - работа LLM (chat)
5. ✅ **Chat JSON Endpoint** - работа LLM (JSON)
6. ✅ **Analyze Site (без сеанса)** - защита от неавторизованных запросов

### Ожидаемый результат:

```
🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀
  ТЕСТИРОВАНИЕ API ГЕНЕРАТОРА ПОСТОВ
🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀 🚀

============================================================
  ТЕСТ 1: Health Check
============================================================
✅ Сервер работает
   Ответ: {'status': 'healthy'}

...

============================================================
  ИТОГИ ТЕСТИРОВАНИЯ
============================================================
  ✅ PASS - Health Check
  ✅ PASS - Root Endpoint
  ✅ PASS - Session Check
  ✅ PASS - Chat Endpoint
  ✅ PASS - Chat JSON Endpoint
  ✅ PASS - Analyze Site (без сеанса)

  Результат: 6/6 тестов пройдено

  🎉 Все тесты пройдены успешно!
```

---

## 👤 Ручное тестирование

### 1. Проверка Frontend

1. Откройте http://localhost:3000
2. Проверьте, что страница загружается
3. Проверьте форму ввода email
4. Проверьте форму оплаты (если сеанс не найден)

### 2. Проверка Backend API

#### Swagger UI (рекомендуется)

1. Откройте http://localhost:8000/docs
2. Проверьте доступные endpoints
3. Протестируйте `/health` endpoint
4. Протестируйте `/llm/chat` endpoint

#### cURL команды

```bash
# Health check
curl http://localhost:8000/health

# Root endpoint
curl http://localhost:8000/

# Session check
curl http://localhost:8000/session/check/test@example.com

# Chat (требует API ключи)
curl -X POST http://localhost:8000/llm/chat \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Привет!", "temperature": 0.0}'
```

---

## ✅ Проверка функциональности

### Полный цикл тестирования:

#### Шаг 1: Создание сеанса (через оплату)

1. Откройте frontend
2. Введите email (например, `test@example.com`)
3. Пройдите процесс оплаты (или создайте тестовый сеанс в БД)

#### Шаг 2: Анализ сайта

1. Введите URL сайта (например, `https://example.com`)
2. Выберите стиль публикации
3. (Опционально) Укажите инфоповод
4. Нажмите "Анализировать сайт"
5. Дождитесь результатов (5-10 секунд)

**Ожидаемый результат:**
- ✅ 3 поста объёмом 400-800 символов
- ✅ Посты соответствуют выбранному стилю
- ✅ Инфоповод учтён (если указан)

#### Шаг 3: Смена стиля

1. Нажмите "Создать новые публикации"
2. Выберите другой стиль
3. Нажмите "Анализировать сайт"
4. Дождитесь результатов (8-12 секунд, быстрее благодаря кэшу)

**Ожидаемый результат:**
- ✅ Генерация быстрее (используется кэш)
- ✅ Новые посты в другом стиле
- ✅ URL и инфоповод заблокированы

#### Шаг 4: Экспорт в DOCX

1. После получения результатов
2. Нажмите "Сохранить отчёт"
3. Проверьте скачанный файл

**Ожидаемый результат:**
- ✅ Файл `.docx` скачан
- ✅ Содержит все 3 поста
- ✅ Содержит метаданные (URL, стиль, инфоповод, дата)

---

## 🔍 Проверка компонентов

### Backend компоненты:

```bash
# Проверка базы данных
python -c "from app.models.database import init_db; init_db(); print('✅ БД инициализирована')"

# Проверка LLM клиента
python -c "from app.services.llm_client import LLMClient; client = LLMClient(); print('✅ LLM клиент инициализирован')"

# Проверка Redis (если используется)
redis-cli ping  # Должен вернуть PONG
```

### Frontend компоненты:

```bash
cd frontend

# Проверка зависимостей
npm list --depth=0

# Проверка сборки
npm run build

# Проверка линтера
npm run lint
```

---

## 🐛 Устранение проблем

### Проблема: "Cannot connect to backend"

**Решение:**
1. Проверьте, что backend запущен: `docker-compose ps`
2. Проверьте логи: `docker-compose logs backend`
3. Проверьте порт: `curl http://localhost:8000/health`

### Проблема: "LLM не отвечает"

**Решение:**
1. Проверьте `.env` файл (API ключи)
2. Проверьте логи backend на ошибки API
3. Проверьте баланс на аккаунтах DeepSeek/ProxyAPI

### Проблема: "Redis connection error"

**Решение:**
1. Проверьте, что Redis запущен: `docker-compose ps redis`
2. Проверьте `REDIS_URL` в `.env`
3. Проверьте логи: `docker-compose logs redis`

### Проблема: "Session not found" при тестировании

**Решение:**
Это нормально! Создайте тестовый сеанс:

```python
from app.models.database import SessionLocal, init_db
from app.services.session_service import session_service

init_db()
db = SessionLocal()
session_service.create_session(db, "test@example.com")
db.close()
```

### Проблема: "Frontend не загружается"

**Решение:**
1. Проверьте, что frontend контейнер запущен
2. Проверьте логи: `docker-compose logs frontend`
3. Проверьте порт: `curl http://localhost:3000`

---

## 📊 Метрики для проверки

### Производительность:

- ✅ Первый анализ: 5-10 секунд
- ✅ Смена стиля: 8-12 секунд
- ✅ Health check: < 100ms
- ✅ Chat endpoint: < 3 секунд

### Функциональность:

- ✅ Генерация 3 постов
- ✅ Объём постов: 400-800 символов
- ✅ 9 стилей доступны
- ✅ Кэширование работает
- ✅ Экспорт DOCX работает

---

## 🎯 Чек-лист перед продакшеном

- [ ] Все автоматические тесты проходят
- [ ] .env файл настроен с реальными ключами
- [ ] База данных инициализирована
- [ ] Redis работает
- [ ] Frontend собирается без ошибок
- [ ] Backend запускается без ошибок
- [ ] Все endpoints доступны
- [ ] Полный цикл (анализ → генерация → экспорт) работает
- [ ] Логи не содержат критических ошибок
- [ ] Документация актуальна

---

## 📝 Дополнительные ресурсы

- **API документация:** http://localhost:8000/docs
- **Swagger UI:** http://localhost:8000/docs
- **ReDoc:** http://localhost:8000/redoc

---

**Удачного тестирования! 🚀**

