# ⚡ БЫСТРОЕ ТЕСТИРОВАНИЕ

## 🚀 Запуск за 3 шага

### Шаг 1: Запустите сервисы

```bash
docker-compose up -d
```

Проверьте, что все контейнеры запущены:
```bash
docker-compose ps
```

Должны быть запущены:
- ✅ `public-backend`
- ✅ `public-frontend`
- ✅ `public-redis`

### Шаг 2: Запустите автоматические тесты

```bash
python test_api.py
```

**Ожидаемый результат:** Все 6 тестов должны пройти ✅

### Шаг 3: Создайте тестовый сеанс (для полного тестирования)

**Важно:** Сеанс нужно создавать в контейнере backend, так как БД находится там.

**Windows (PowerShell):**
```powershell
.\create_session.ps1 test@example.com
```

**Linux/Mac:**
```bash
chmod +x create_session.sh
./create_session.sh test@example.com
```

**Или напрямую через Docker:**
```bash
docker-compose exec backend python -c "from app.models.database import init_db, SessionLocal; from app.services.session_service import session_service; import os; os.environ['DB_PATH'] = '/app/data/sessions.db'; init_db(); db = SessionLocal(); session = session_service.create_session(db, 'test@example.com'); print('Сеанс создан:', session.email); db.close()"
```

Теперь можно протестировать полный функционал через frontend!

---

## 📋 Чек-лист тестирования

### ✅ Базовые проверки

- [ ] Backend доступен: http://localhost:8000/health
- [ ] Frontend доступен: http://localhost:3000
- [ ] API документация: http://localhost:8000/docs
- [ ] Все автоматические тесты проходят

### ✅ Функциональные проверки

- [ ] Создан тестовый сеанс
- [ ] Frontend показывает форму анализа
- [ ] Анализ сайта работает (5-10 сек)
- [ ] Генерируются 3 поста (400-800 символов)
- [ ] Смена стиля работает (8-12 сек)
- [ ] Экспорт DOCX работает
- [ ] Кэширование работает (быстрая смена стиля)

### ✅ Проверка защиты

- [ ] Анализ без сеанса возвращает 403
- [ ] Истекший сеанс возвращает 410
- [ ] Использованный стиль блокируется

---

## 🔧 Быстрые команды

```bash
# Проверка логов
docker-compose logs -f backend

# Перезапуск сервисов
docker-compose restart

# Остановка
docker-compose down

# Полная пересборка
docker-compose up --build -d
```

---

## 🐛 Если что-то не работает

1. **Backend не запускается:**
   ```bash
   docker-compose logs backend
   ```
   Проверьте `.env` файл с API ключами

2. **Frontend не загружается:**
   ```bash
   docker-compose logs frontend
   ```

3. **Тесты не проходят:**
   - Убедитесь, что backend запущен
   - Проверьте API ключи в `.env`
   - Проверьте логи: `docker-compose logs backend`

---

**Подробная документация:** [TESTING_GUIDE.md](TESTING_GUIDE.md)

