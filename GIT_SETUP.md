# Настройка Git для деплоя на VPS

## ✅ Что было сделано:

1. ✅ Инициализирован Git репозиторий
2. ✅ Обновлен `.gitignore` (исключены системные файлы, БД, venv, node_modules)
3. ✅ Создан первый коммит с исходным кодом
4. ✅ Созданы скрипты деплоя:
   - `deploy.sh` - для Linux/VPS
   - `deploy.ps1` - для Windows (локально)
5. ✅ Создана инструкция `DEPLOY.md` с подробным описанием процесса

## 📋 Следующие шаги:

### 1. Создание удаленного репозитория (опционально, но рекомендуется):

```bash
# На GitHub/GitLab создайте новый репозиторий, затем:
git remote add origin <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
git branch -M main
git push -u origin main
```

### 2. Настройка VPS:

#### Подключитесь к VPS по SSH и выполните:

```bash
# Установка Docker и Docker Compose (если еще не установлены)
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo apt install docker-compose -y

# Клонирование репозитория
mkdir -p ~/projects
cd ~/projects
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ> public_gen
cd public_gen/public

# Или если репозиторий еще не создан, скопируйте файлы вручную:
# scp -r * user@vps:~/projects/public_gen/public/
```

#### Создайте `.env` файл на VPS:

```bash
nano .env
```

Добавьте необходимые переменные:
```
W=60
Price=1000
ShopID=your_shop_id
Ykassa_API=your_api_key
OPENAI_API_KEY=your_openai_key
REDIS_URL=redis://redis:6379
DB_PATH=sessions.db
```

### 3. Деплой на VPS:

```bash
# Перейти в директорию проекта
cd ~/projects/public_gen/public

# Сделать скрипт исполняемым
chmod +x deploy.sh

# Запустить деплой (это удалит старые контейнеры и создаст новые)
./deploy.sh
```

## 🔄 Процесс обновления:

### Локально (после изменений):

```bash
# 1. Внести изменения в код
# 2. Закоммитить
git add .
git commit -m "Описание изменений"
git push origin main
```

### На VPS (обновление):

```bash
# 1. Перейти в директорию проекта
cd ~/projects/public_gen/public

# 2. Обновить код из Git
git pull origin main

# 3. Запустить деплой (удалит старые контейнеры и создаст новые)
./deploy.sh
```

## 📝 Важные замечания:

1. **Скрипт `deploy.sh` автоматически:**
   - Останавливает и удаляет старые контейнеры
   - Удаляет старые образы
   - Обновляет код из Git (если используется)
   - Собирает новые образы
   - Запускает новые контейнеры

2. **База данных сохраняется:**
   - Файл `sessions.db` находится в томе Docker
   - При удалении контейнеров данные не теряются (если том не удален)

3. **Redis данные сохраняются:**
   - Данные Redis хранятся в томе `redis-data`
   - История параметров сохраняется между перезапусками

## 🔍 Проверка работы:

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f

# Проверка здоровья
curl http://localhost:8000/health
```

## 📚 Дополнительная информация:

Подробные инструкции см. в файле `DEPLOY.md`

